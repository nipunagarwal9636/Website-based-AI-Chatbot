jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # ...
      - name: Run script with API Key
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: |
          python app.py
